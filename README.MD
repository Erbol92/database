# Домашнее задание «Работа с PostgreSQL. Создание БД»

```
В схеме и запросах у вас есть пару неточностей:
в таблице исполнителей поле с жанром совсем не нужно, т.к. за связь отвечает промежуточная таблица;
аналогично с полем исполнителей в таблице альбомов;
И рекомендовал бы больше внимания обращать на создание ограничений, которые будут гарантировать корректность данных в таблицах. Например, можно было сделать ограничение на год выхода альбомов/сборников (не менее определенного числа), продолжительность треков и пр.
А для поля продолжительности в таблице треков лучше использовать тип integer – длительность в секундах, далее так проще будет работать с этим полем для различных вычислений.
И просьба все файлы с кодом все же сдавать ссылками на github, как вы делали в курсах по программированию. Это такой же код, лучше использовать репозиторий :)
```
### Приложил старую схему и скрипт